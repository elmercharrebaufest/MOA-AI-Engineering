# Briefing para el developer — exploración libre sobre tarea real (ARMOA277-44)

## Contexto que sí puede tenerse

Usted es developer del proyecto "AWS Portal de Créditos". Tiene asignada la tarea real
**ARMOA277-44**. Nunca usó `MOA-AI-Engineering`. No sabe qué hace ni para qué sirve.

> Mientras trabaja en ARMOA277-44 (con el asistente de IA que use normalmente — Copilot,
> Claude, etc.), dese una vuelta por el repositorio `MOA-AI-Engineering` (sibling en este
> mismo workspace) y observe si hay algo ahí que sirva para esa tarea puntual. No hay una
> capacidad asignada de antemano — usted decide qué mirar, qué probar, qué ignorar.

No hay respuesta esperada ni resultado "correcto". Esto es exploratorio: la pregunta real
es si el modelo aporta algo o no, y qué.

## Qué anotar durante la exploración

- Qué se mira primero (README, Registry, `capabilities/`, `golden-paths/`, otra cosa).
- Qué se prueba concretamente (un `INSTRUCTIONS.md`, un prompt, un golden path, una
  Evidence/Evaluation, etc.) y en qué parte de la tarea se aplica.
- Si algo ahorra tiempo, evita un error, o da una respuesta que no se tenía.
- Si algo resulta irrelevante para la tarea, confuso, o simplemente no se entiende.
- Si en algún momento se prefiere seguir sin usar nada de esto — corresponde anotarlo
  también, es feedback válido.

## Qué no hace falta

- No hace falta terminar ni entregar ARMOA277-44 usando esto — es un experimento
  aislado (ver `README.md` de esta carpeta).
- No hace falta seguir ningún orden ni cubrir todo el repo.

## Qué se va a preguntar después (adelanto, no responder ahora)

Ver `feedback-record.md` — son preguntas abiertas sobre qué se usó,
qué aportó y qué no. **(Nota: este piloto se cerró sin ejecutar — ver `README.md` de
esta carpeta — el archivo se eliminó, no se re-crea acá.)**

## Nota para quien coordina el piloto

Cuando el developer termine (o llegue a un punto natural de corte), corresponde
completar `execution-record.md` y `feedback-record.md` con sus respuestas literales —
no deben resumirse ni completarse en su nombre.
