# Evaluation Record — EXEC-20260908-005

**Limitación explícita, sin excepción**: esta evaluación fue realizada por el mismo actor
que ejecutó el vertical slice — **no es una evaluación humana independiente**. Se registra
con el método declarado honestamente (`model-assisted`), sin sustituir el HITL real que
[`user-story/SKILL.md`](../capabilities/skills/user-story/SKILL.md) exige antes de
Planning/desarrollo.

## Evaluation Contract

| Campo | Valor |
|---|---|
| `capability_id` | CAP-002, habilitado por CAP-008 |
| `evidence_reference` | [`../evidence/EXEC-20260908-005.md`](../evidence/EXEC-20260908-005.md) |
| `criteria` | Las mismas 9 preguntas usadas en `evaluation/EXEC-20260908-004.md`, para permitir comparación directa |
| `method` | `model-assisted` — **no** `human` |
| `evaluator` | Ejecutor de esta actividad — **REQUIRES HUMAN VALIDATION** para evaluación independiente real |
| `result` | `PARTIAL` — ver justificación |
| `evaluated_at` | 2026-09-08 |
| `hitl_required` | `true` |
| `hitl_confirmed_by` | Ninguno — **REQUIRES HUMAN VALIDATION** |

## Las 9 preguntas (mismas que `EXEC-20260908-004`, para comparar)

| # | Pregunta | Resultado | Justificación |
|---|---|---|---|
| 1 | ¿Se recuperó correctamente vía MCP (Prioridad 1)? | PASS | `getJiraIssue` real, issue real `ARMOA277-180`, verificable en `https://baufest.atlassian.net/browse/ARMOA277-180` |
| 2 | ¿El Resolved Context es completo respecto al contrato? | **PARTIAL** | Cumple `resolved-context.schema.json` (todos los campos requeridos presentes), pero `description`/`requirements`/`acceptanceCriteria` son `null` porque el issue real no tiene esos campos — más vacío que `EXEC-20260908-004` |
| 3 | ¿CAP-002 pudo consumirlo? | PASS | Ejecutó sin error, produjo historia + gaps aun con input mínimo — no se bloqueó por falta de descripción |
| 4 | ¿Se preservó el significado del requerimiento? | PASS | Único dato real disponible (`summary`) preservado textualmente, sin agregar detalle no presente |
| 5 | ¿Se inventó información? | **PASS (negativo — no se inventó nada)** | Sin descripción, no se generó ningún criterio de aceptación ni regla de negocio — se registraron como gap en vez de rellenarse con contenido plausible |
| 6 | ¿Los gaps detectados corresponden realmente al issue? | PASS | Los 6 gaps son verificables contra la ausencia real de campos en el issue, no son gaps genéricos de plantilla |
| 7 | ¿Existe trazabilidad completa? | PASS | `getJiraIssue` (MCP) → Resolved Context (JSON real) → input de CAP-002 → salida, cada paso citado en `evidence/EXEC-20260908-005.md` |
| 8 | ¿Se usó exclusivamente el camino MCP (Prioridad 1), sin REST/API Token como fallback? | PASS | Ningún uso de `jira-context.ps1` ni de variables `JIRA_*` en esta ejecución |
| 9 | ¿Se realizó alguna operación `WRITE` sobre Jira? | PASS (negativo — ninguna operación WRITE) | Solo se invocaron herramientas READ (`searchJiraIssuesUsingJql`, `getJiraProjectIssueTypesMetadata`, `getJiraIssue`) |

**Resultado agregado**: `PARTIAL` — mismo resultado que `EXEC-20260908-004`, por las mismas
2 razones: evaluación no independiente/humana (regla dura), y la pregunta 2 matizada por
una limitación real de la fuente (acá más severa: ausencia total de descripción, no solo
de Acceptance Criteria). No se redondeó hacia arriba.

## Diferencia clave frente a `EXEC-20260908-004` en esta evaluación

En `EXEC-20260908-004`, la pregunta 2 quedó `PARTIAL` solo por la ausencia de
`acceptanceCriteria`/`requirements` (campos que un bug normalmente no tiene). Acá, la
pregunta 2 queda `PARTIAL` por una ausencia más profunda: **tampoco hay `description`** —
el issue es solo un título. Esto no cambia el resultado agregado (`PARTIAL` en ambas), pero
sí confirma que la limitación de completitud del Resolved Context **escala con la
completitud real del issue de origen**, no es un defecto del patrón/contrato.

## Separación explícita (mismo principio que `EXEC-20260908-004`)

- **Ejecución**: confirmada, real, con herramientas MCP reproducibles
  (`evidence/EXEC-20260908-005.md`).
- **Evaluación humana**: **no existe** — `hitl_confirmed_by: Ninguno`.
- **Evaluación asistida** (esta): `model-assisted`, declarada como tal, no presentada como
  independiente en ningún campo.
